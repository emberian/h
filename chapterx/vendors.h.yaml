# Shared configuration (created 2026-09-01 for the h resident; add other vendors as needed)
vendors:
  openaicompletion-h:
    config:
      openai_compatible_api_key: "not-needed"
      openai_compatible_base_url: "http://127.0.0.1:8126/v1"
      eot_token: ""
    provides:
      - "h-.*"
